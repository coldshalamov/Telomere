# Telomere Risk Register

Severity: H / M / L. Status: open / mitigated / closed. Every risk carries a
next test and a kill condition. Updated for the June 2026 proof-kernel
revision (layer-masked expansion, LITERAL_RUN, J2D1, walk-DP grid pricing).

| # | risk | sev | status | mitigation | next test | kill condition |
| - | --- | - | --- | --- | --- | --- |
| 1 | **Uniform-hash-law gap** — every viability number rides `P(match)=2^-S`; a real expander could correlate across positions/masks | H | open | law validated at toy scale with real SHA-256 dice (sustained masked rate matches fresh=1 prediction; no-refresh decays exactly as predicted) | scale the dice test 10–100× on a workstation; vary expander (SHA-256, BLAKE3, AES-CTR); χ² on match-count distributions | measured sustained rate < 0.5× kernel prediction at any scale ⇒ re-derive law or kill masked lane |
| 2 | **Uncharged passthrough** (the classic failure) | H | mitigated | hard gate in both kernels; every run/single header charged in its emitting layer; ledger row `uncharged_passthrough` asserted false; dead lanes documented | adversarial audit of `vnext_kernel.run_pass` charging by a second agent | any output bit not regenerated by a record or carried under a charged header |
| 3 | **Model/implementation mismatch** — expectation-level recurrence vs a real encoder (selection conflicts, integer effects, histogram drift) | H | open | two independently built kernels agree within ~25% (v-next conservative); greedy ≈ oracle bound flagged per config | 60-day milestone: tiny end-to-end v-next encoder; compare measured pass ledger vs model rows | implemented rate < 0.5× modeled rate after debugging ⇒ recalibrate or demote primary |
| 4 | **v-next bookkeeping approximations** (avg-length run model, proportional removals, wire-period accounting) | H | partially mitigated | THREE incidents found and fixed by instrumented cross-checks (short-run walk laundering; payload-step header crossing; apply-step triple-dip — the last retracted a 0.908%/pass headline). Hand-derived channel arithmetic now matches kernel output on diagnostics | Monte-Carlo a 10k-entry toy layer against the recurrence each revision; keep the hand-check diagnostic in CI | any further >1.5× discrepancy between hand-derived and kernel rates ⇒ freeze headlines until reconciled |
| 5 | **Epoch-channel dependency** (was: masked freshness too good to be true — that kill condition FIRED via maintainer review: masks need pass-varying keys nothing can derive at decode; primary fell to the permutation family exactly as written) | M | mitigated by construction (June 2026: `v1_roundtrip_proof.py` 36/36 — birth inference + salt replay proven at toy scale; residual = Q1 analytic bound at scale) | dependency map in the frontier report; arity-1 length-preservation lemma narrows the need to bundles only; affine-stride fingerprint is the candidate channel; explicit charging priced and negative | the stride-induction toy proof (three passes, pass-2 bundle inside pass-3 bundle, decode by inference, wire==charged, escape ledger exact) — the maintainer's demanded test, now the 30-day cornerstone | induction not well-founded or escapes not ~T/N ⇒ refreshed lanes lose their decode story ⇒ program falls to no-refresh (dead) or charged-epoch (negative) |
| 6 | **Lotus cost mismatch** (127-vs-508; tier-field layout) | M | open | repo-internal evidence pins 508; reference layout fixed (tw−1); golden vectors in `cost_pin_report.json`; operating range (≤161-bit payloads) unaffected either way | `cargo run --quiet --bin v1_cost_table` locally + golden vectors vs `../lotus` crate | lotus crate disagrees with `lotus_width_for_value` arithmetic ⇒ re-pin every cost and re-run sweeps |
| 7 | **BIT_LITERAL / LITERAL_RUN / masked-expansion decode at real wire level** | M | open | toy decode proofs pass (round trip, self-delimiting, charged==wire, nested, adversarial payloads, odd tails) | Rust reference primitives + `cargo test` round trips (60-day) | any wire-level counterexample ⇒ demote the primitive and every config using it |
| 8 | **Working-state explosion** (superposition) | L | mitigated | branching measured subcritical (~2^-7/generation); cap 4 captures everything; contribution small (+0.003..0.016 pp) | none needed at current settings | n/a |
| 9 | **Compute cost** — 81–500 passes × ~10M lookups/MB | M | open | masked targets keep tables shared (2^28 build once); per-pass cost is lookup-bound, not expansion-bound; tier table in the brief | benchmark one model-equivalent pass on CPU and GPU (60-day) | $/TB at crossover exceeds storage savings by >10× on best 3-yr hardware path ⇒ archive-tier business case fails |
| 10 | **Post-500-pass extrapolation** | M | open | all headline numbers stop at 500; no extrapolation claimed anywhere | extend primary recurrence to 2000 passes as a labeled curiosity, never a headline | n/a (policy risk) |
| 11 | **Stale docs / contradictory reports** | M | mitigated | single ledger (`TELOMERE_RESULT_LEDGER.md`) declared source of truth; viability/frontier rewritten; README updated; old headline numbers only appear with `invalid` labels | grep sweep for "0.53%", "rechunk-4", "J3D2" claims each revision | a reviewer finds two live documents disagreeing on a number |
| 12 | **Agent reward-hacking** (optimizing the model instead of the mechanism) | M | mitigated-in-practice | evidence-class labels mandatory; oracle rows segregated; conservative discounts; the audit loop has now retracted its own headline twice when checks failed (walk-DP fix; triple-dip fix) — the incentives point the right way | independent re-derivation of `m_records` and the freshness laws by a second agent/reviewer | any constant found tuned to make a target pass rather than derived from format/counting |
| 13 | **Refresh decodability of future operators** | L | mitigated | refresh contract (story A/B) enforced in both kernels; masked schedule is story B with zero metadata | apply the same contract to any new operator | operator needs file-specific state that cannot be charged |
