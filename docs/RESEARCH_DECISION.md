# Telomere Research Decision

Generated by `scripts/generate_research_decision.py` from checked-in queue, audit, and gate artifacts.
This is a downstream decision ledger. It performs no seed search and makes no compression claim.

## Current Decision

- Decision: `hold_gated_compute_and_continue_evidence_maintenance`
- Verdict: `research-viable, not production-proven`
- Overall status: `architecture-implemented; research-viable; not production-proven`
- Ready ungated experiments: `0`
- Top ready lane: `none`
- Gated lanes: `2`
- Blocked-by-evidence lanes: `3`
- Unresolved evidence gates: `27`
- Production proven: `False`

No ready ungated experiments remain; do not spend on gated compute or production GPU until evidence improves.

## Completion Boundary

- Completion boundary: `goal_not_complete_until_natural_corpus_or_supported_production_claims_are_proven`
- The active research goal is not complete while generalized natural-corpus compression, production acceleration, or supported production-format claims remain unproved.
- Planted/generative wins prove the mechanism can work, not that arbitrary natural data is compressible.

## Blocked Actions

| lane | status | do not do | stop rule |
| --- | --- | --- | --- |
| `long-span-bundle-gate` | `blocked-by-evidence` | Use the generated long-span bundle gate as the source of truth; do not run broad long-span sweeps while it reports 0 of 8 gates met. | Do not run broad long-span bundle sweeps until search frontier, raw-suffix break-even, selected-span, and control gates pass. |
| `format-transforms` | `blocked-by-evidence` | Do not add transform metadata to `.tlmr` yet. | Reject format work if exact hits remain absent after held-out validation. |
| `gpu-acceleration` | `blocked-by-evidence` | Do not implement production GPU acceleration until CPU evidence identifies a worthwhile target workload. | Stop GPU work if it cannot beat CPU streaming on the promoted workload under parity tests. |
| `heldout-corpora` | `gated` | Keep replication corpora in a separate frontier artifact unless the team intentionally accepts the expensive matrix-regeneration cascade. | Keep the separate artifact as the source of truth while raw prefix>=5, exact-hit, and selected-span rows remain zero. |
| `search-depth` | `gated` | Keep broad depth-3 structured sweeps gated after the bounded compression follow-up returned null. | Abort deeper search if frontier exact discovery remains prefix-only with no exact hits. |

## Reopen Triggers

| lane | status | promotion gate | suggested artifact |
| --- | --- | --- | --- |
| `long-span-bundle-gate` | `blocked-by-evidence` | Open this lane only when LONG_SPAN_BUNDLE_GATE reports every prerequisite gate met or an intentional override is documented with new evidence. | Use `docs/LONG_SPAN_BUNDLE_GATE.md`; only replace this with a sweep artifact after the generated gate passes. |
| `heldout-corpora` | `gated` | Promote into canonical matrices only for accepted coverage expansion or after unrelated ordinary groups produce prefix>=5 movement, exact hits, or selected spans while controls stay null. | Use `docs/HELDOUT_CORPUS_EXPANSION.md`; schedule matrix integration only with an explicit regeneration budget. |
| `search-depth` | `gated` | Promote broader depth-3 or opt-in depth-4 work only if a new lead finds prefix>=6 movement, exact hits, selected spans, negative delta, or forecast scale below 1 GiB for one expected exact hit. | Use `docs/SEARCH_FRONTIER_GATE.md` as the broad go/no-go gate before any raw depth or full depth-4 proposal. |
| `format-transforms` | `blocked-by-evidence` | Only draft a format extension after a reversible transform produces repeatable exact seed-span wins. | Keep ADR-0001 policy in force. |
| `gpu-acceleration` | `blocked-by-evidence` | A CPU workload shows repeatable exact hits but is too slow, then GPU parity and benchmark work becomes valuable. | Keep `docs/ACCELERATION.md` as the GPU promotion gate. |

## Long-Span Gate Details

- Long-span checks met: `0` / `8`
- Recommendation: `hold_long_span_bundle_sweeps`
- Search frontier status: `hold-broad-depth-search`
- Broad depth search allowed: `False`
- Best non-planted forecast: `828.0` GiB per expected exact hit

| gate | observed | consequence |
| --- | --- | --- |
| `search-frontier-open` | hold-broad-depth-search | Do not run long-span raw-depth sweeps while the frontier gate is closed. |
| `forecast-scale` | 828.0 GiB | Long-span search is not compute-rational from the current forecast. |
| `selected-span-frontier` | 0 selected spans | Do not optimize long-span bundle packing before profitable spans exist. |
| `raw-suffix-break-even` | observed prefix 4 vs required 6 | Prefix-4 movement remains steering evidence, not long-span bundle evidence. |
| `short-hit-controls` | 3 ordinary groups, 1 control groups, control density comparable True | Current exact short-hit density is too control-like to justify broad long-span work. |
| `whole-stream-residual` | 0 ordinary groups, 0 control groups | Residual-vector evidence does not yet support long-span bundle promotion. |
| `seed-table-generalization` | seed-table ordinary groups 0; schema replication controls 2; claim control_failed_on_frozen_expansion_corpora | Public dictionary signals stay research-only until replication controls pass. |
| `packed-replication` | 0 full-stream rows, 0 ordinary groups | Descriptor packing remains narrow and does not justify broad long-span bundle sweeps. |

## Allowed Maintenance

- regenerate checked evidence artifacts when an upstream source changes
- run verification gates and fix stale hashes or stale wording
- tighten documentation around non-claims, stop rules, and compatibility boundaries
- pre-register new corpus or preset manifests without claiming compression
- use subagents for independent read-only audits or disjoint low-risk patches

## Source Artifacts

- `experiment_queue_sha256`: `e8dd0c4c01f857ef191d23894e008f96744d39fa71e9803e114030dc1aa68402`
- `goal_audit_sha256`: `5e9179c80ce73b8a34592643145f9344c3b96bffd457c96d9d5b97193e63ec70`
- `research_scorecard_sha256`: `165f9354ac1f0d81f4f7886ada309e6ef7484fd0abc2ca34dd3eced24d1f61dc`
- `viability_sha256`: `94d60bfcebc377e82c3d9c1c409ecff81708ef679c61f7b00f917b0856ac2ff4`
- `long_span_bundle_gate_sha256`: `290b4a0817f5c0fbd875dd7f010a15113acc0aaa56426226d334e6dcf73cf5b1`
- `search_frontier_gate_sha256`: `aab9351032df10d7998c3a0e04306bb9d0f344b5d0556cc721592c7b8793737f`
- `acceleration_report_sha256`: `623b59981cf78e7a22d54fb4111b663fc2975c5a923ae3bf8ff288d9f42b1207`
- `scale_performance_report_sha256`: `b14646b969fc65e6c3c52584107e3478ed6e28671e6f0ce058f70f80d13819f7`
- `recursive_structured_fixtures_sha256`: `74f7cd8ae7350ad130c17996b0724f251168638fce3325079d71a96deccf3614`
