# Telomere Research Frontier

Generated by `scripts/generate_research_frontier.py` from checked-in decision, queue, audit, and gate artifacts.
This is a No Seed Search artifact: it performs no seed search, launches no compute, is not a compression claim, adds no `.tlmr` format support, and does not override SEARCH_FRONTIER_GATE.

## Compute Posture

- Frontier status: `no_ungated_compute_ready`
- Decision: `hold_gated_compute_and_continue_evidence_maintenance`
- Completion boundary: `goal_not_complete_until_natural_corpus_or_supported_production_claims_are_proven`
- Verdict: `research-viable, not production-proven`
- Ready ungated experiments: `0`
- Gated lanes: `2`
- Blocked-by-evidence lanes: `3`
- Qualified lanes: `3`
- Unresolved evidence gates: `27`
- Long-span checks met: `0` / `8`
- Search frontier status: `hold-broad-depth-search`
- Broad depth search allowed: `False`
- Best non-planted forecast: `828.0` GiB per expected exact hit
- Ungated compute allowed: `False`
- Production proven: `False`
- Allowed maintenance only: `True`

There is no ready ungated compute: No ready ungated experiments remain; do not spend on gated compute, production GPU, or format promotion until a generated reopen trigger changes.

## Override Policy

A gated-compute override requires a new generated evidence artifact, an explicit compute budget, and preserved stop rules; this artifact does not override SEARCH_FRONTIER_GATE.

## Reopen Trigger Matrix

| lane | status | promotion gate | suggested artifact |
| --- | --- | --- | --- |
| `long-span-bundle-gate` | `blocked-by-evidence` | Open this lane only when LONG_SPAN_BUNDLE_GATE reports every prerequisite gate met or an intentional override is documented with new evidence. | Use `docs/LONG_SPAN_BUNDLE_GATE.md`; only replace this with a sweep artifact after the generated gate passes. |
| `heldout-corpora` | `gated` | Promote into canonical matrices only for accepted coverage expansion or after unrelated ordinary groups produce prefix>=5 movement, exact hits, or selected spans while controls stay null. | Use `docs/HELDOUT_CORPUS_EXPANSION.md`; schedule matrix integration only with an explicit regeneration budget. |
| `search-depth` | `gated` | Promote broader depth-3 or opt-in depth-4 work only if a new lead finds prefix>=6 movement, exact hits, selected spans, negative delta, or forecast scale below 1 GiB for one expected exact hit. | Use `docs/SEARCH_FRONTIER_GATE.md` as the broad go/no-go gate before any raw depth or full depth-4 proposal. |
| `format-transforms` | `blocked-by-evidence` | Only draft a format extension after a reversible transform produces repeatable exact seed-span wins. | Keep ADR-0001 policy in force. |
| `gpu-acceleration` | `blocked-by-evidence` | A CPU workload shows repeatable exact hits but is too slow, then GPU parity and benchmark work becomes valuable. | Keep `docs/ACCELERATION.md` as the GPU promotion gate. |

## Frontier Trigger Board

| frontier | status | canonical artifact | current observation | promotion trigger | unlocks if met | still forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| `long-span-bundle-gate` | `blocked-by-evidence` | `docs/LONG_SPAN_BUNDLE_GATE.md` | 0 of 8 gates met; recommendation hold_long_span_bundle_sweeps. | Open this lane only when LONG_SPAN_BUNDLE_GATE reports every prerequisite gate met or an intentional override is documented with new evidence. | a narrow long-span bundle sweep artifact, not broad production claims | broad long-span sweeps; format promotion; production compression claims |
| `heldout-corpora` | `gated` | `docs/HELDOUT_CORPUS_EXPANSION.md` | 20 frozen corpora; prefix>=5 rows 0; exact hits 0; selected spans 0. | Promote into canonical matrices only for accepted coverage expansion or after unrelated ordinary groups produce prefix>=5 movement, exact hits, or selected spans while controls stay null. | canonical corpus-matrix integration with explicit regeneration budget | expensive matrix regeneration by default; natural-corpus compression claims |
| `search-depth` | `gated` | `docs/SEARCH_FRONTIER_GATE.md` | status hold-broad-depth-search; selected spans 0; forecast 828.0 GiB. | Promote broader depth-3 or opt-in depth-4 work only if a new lead finds prefix>=6 movement, exact hits, selected spans, negative delta, or forecast scale below 1 GiB for one expected exact hit. | broader depth-3 or opt-in depth-4 proposal | broad raw depth search; full depth-4 execution; compute escalation without exact-hit evidence |
| `format-transforms` | `blocked-by-evidence` | `docs/adr/0001-transform-preconditioners.md` | 11 transform/channel/dictionary blockers remain blocked-by-evidence. | Only draft a format extension after a reversible transform produces repeatable exact seed-span wins. | draft transform metadata proposal outside stable v1 | .tlmr transform metadata; format support claims; counting transform-only shortening as Lotus compression |
| `gpu-acceleration` | `blocked-by-evidence` | `docs/ACCELERATION.md` | acceleration status research-only-cpu-semantics; real kernel detected False. | A CPU workload shows repeatable exact hits but is too slow, then GPU parity and benchmark work becomes valuable. | real GPU parity and benchmark work | production GPU acceleration; trusting GPU output without CPU parity |

## Next Evidence Triggers

| trigger | meaning |
| --- | --- |
| `prefix>=6` | held-out generated-prefix movement strong enough to revisit raw-suffix economics |
| `exact_hits>0` | a non-planted target span exactly equals a generated seed prefix |
| `selected_spans>0` | the compressor selected profitable seed-span records after metadata |
| `forecast_gib<1` | expected exact-hit cost is low enough to revisit broad depth search |
| `ordinary_negative_groups_with_null_controls` | negative delta appears in unrelated ordinary groups while controls stay null |

## Frontier Lanes

| rank | lane | status | group | stop rule |
| ---: | --- | --- | --- | --- |
| 1 | `long-span-bundle-gate` | `blocked-by-evidence` | `corpus-transform` | Do not run broad long-span bundle sweeps until search frontier, raw-suffix break-even, selected-span, and control gates pass. |
| 2 | `heldout-corpora` | `gated` | `corpus-transform` | Keep the separate artifact as the source of truth while raw prefix>=5, exact-hit, and selected-span rows remain zero. |
| 3 | `search-depth` | `gated` | `compute-economics` | Abort deeper search if frontier exact discovery remains prefix-only with no exact hits. |
| 4 | `scale-performance` | `qualified` | `compute-economics` | Stop scaling if peak working set grows faster than search-work and target-table telemetry explains, or if target-table estimates exceed the configured memory limit. |
| 5 | `format-transforms` | `blocked-by-evidence` | `format-policy` | Reject format work if exact hits remain absent after held-out validation. |
| 6 | `gpu-acceleration` | `blocked-by-evidence` | `acceleration` | Stop GPU work if it cannot beat CPU streaming on the promoted workload under parity tests. |
| 7 | `tauri-workflow` | `qualified` | `operator-ui` | Stop UI expansion if it encourages unsupported compression claims or hides null results. |
| 8 | `research-audit` | `qualified` | `meta-research` | Stop adding new experiment families when they do not change the scorecard or queue. |

## Unresolved Requirement Clusters

| section | unresolved count | status counts | first remaining gate |
| --- | ---: | --- | --- |
| `affine-transform-search` | 1 | blocked-by-evidence=1 | Treat prefix-4 movement as a steering clue only; require residual-sidecar savings, prefix>=5, or exact hits before depth escalation. |
| `alignment-arity-discovery` | 1 | blocked-by-evidence=1 | Find profitable exact hits or prefix>=5 movement before spending more on alignment-only tuning. |
| `bwt-mtf-transform-probe` | 1 | blocked-by-evidence=1 | Treat BWT/MTF/RLE as null seed-span evidence unless it produces prefix>=5, exact hits, or metadata-profitable selected spans. |
| `byte-permutation-transform-search` | 1 | blocked-by-evidence=1 | Treat byte-distribution alignment as null until it produces prefix>=5, exact hits, or metadata-profitable selected spans. |
| `composed-transform` | 1 | blocked-by-evidence=1 | Reject composition as a promotion path unless it produces held-out prefix>=5 or exact seed-span events. |
| `contextual-fifth-byte-steering` | 1 | blocked-by-evidence=1 | Reject contextual steering as a promotion path unless it beats null controls on held-out prefix>=5 or exact hits. |
| `depth3-frontier-exact-discovery` | 1 | blocked-by-evidence=1 | Keep depth 4 opt-in and sharded until this gate finds prefix>=6 movement, exact hits, or selected spans. |
| `depth4-pilot-shard` | 1 | blocked-by-evidence=1 | Keep remaining depth-4 shards gated unless a pilot or upstream lead produces prefix>=6, exact hits, or selected spans. |
| `exact-short-hit-bundle-economics` | 1 | blocked-by-evidence=1 | Do not promote span-3 hit bundling unless full-stream wins survive unrelated ordinary groups with null controls and non-comparable control density. |
| `expander-salt-ensemble` | 1 | blocked-by-evidence=1 | Do not promote salted expanders unless they beat equivalent random trials and create full-stream ordinary held-out wins with null controls. |
| `fifth-byte-steering` | 1 | blocked-by-evidence=1 | Reject residual masks unless they generalize into prefix>=5 or exact hits. |
| `grammar-channel-match-discovery` | 1 | blocked-by-evidence=1 | Treat grammar-channel sidecar models as null until a parser-backed channel survives held-out/control promotion. |
| `lead-exact-discovery` | 1 | blocked-by-evidence=1 | Stop broadening selected prefix-4 leads until a new family or deeper seed lane crosses prefix>=5 or exact-hit thresholds. |
| `long-span-bundle-gate` | 1 | blocked-by-evidence=1 | Do not run broad long-span bundle sweeps until the generated gate passes. |
| `match-discovery` | 1 | blocked-by-evidence=1 | Change match-discovery strategy before returning to sidecar packing or format work. |
| `natural-corpus` | 1 | blocked-by-evidence=1 | Promote only after repeatable prefix>=5 uplift or exact seed-span hits on held-out data. |
| `numeric-value-channel-match-discovery` | 1 | blocked-by-evidence=1 | Treat parsed numeric value channels as null until exact seed spans survive full reconstruction metadata. |
| `packed-sidecar-replication` | 1 | blocked-by-evidence=1 | Find new independent match-discovery leads before more descriptor packing work. |
| `record-context-transform-search` | 1 | blocked-by-evidence=1 | Treat the current record-context lane as null until exact seed spans appear after metadata. |
| `recursive-structured-fixtures` | 1 | blocked-by-evidence=1 | Do not generalize recursive v2 gains unless ordinary non-offset fixtures produce verified later-layer container wins. |
| `schema-native-public-dictionary-replication` | 1 | blocked-by-evidence=1 | Do not promote a public dictionary registry or format-level support until paired shadow controls and external corpora separate cleanly. |
| `seed-manifold-residual-steering` | 1 | blocked-by-evidence=1 | Do not promote residual steering until sidecar bytes plus metadata beat literal storage on held-out corpora. |
| `seed-table-preset-probe` | 1 | blocked-by-evidence=1 | Do not promote a public Lotus preset unless ordinary held-out negative groups reach the gate and controls remain null. |
| `structural-transform-search` | 1 | blocked-by-evidence=1 | Keep format-transform and broad depth work gated until structural or successor transforms produce repeatable promotion signals. |
| `token-dictionary-transform-search` | 1 | blocked-by-evidence=1 | Treat the current token-dictionary lane as null until exact seed spans appear after metadata. |
| `transformed-match-discovery` | 1 | blocked-by-evidence=1 | Find a new transform family or deeper seed lane before promoting transform metadata. |
| `whole-stream-residual-vector-probe` | 1 | blocked-by-evidence=1 | Do not continue residual-sidecar promotion unless whole-stream vector rows produce unrelated ordinary held-out wins with controls null. |

## Unresolved Requirements

| section | status | requirement | remaining gate |
| --- | --- | --- | --- |
| `natural-corpus` | `blocked-by-evidence` | Find repeatable natural or transformed structured-corpus seed-span wins. | Promote only after repeatable prefix>=5 uplift or exact seed-span hits on held-out data. |
| `composed-transform` | `blocked-by-evidence` | Test whether context/residual transforms and periodic masks work better when composed. | Reject composition as a promotion path unless it produces held-out prefix>=5 or exact seed-span events. |
| `depth3-frontier-exact-discovery` | `blocked-by-evidence` | Enumerate the full depth-3 frontier on frozen depth-2 null/frontier rows before considering depth 4. | Keep depth 4 opt-in and sharded until this gate finds prefix>=6 movement, exact hits, or selected spans. |
| `depth4-pilot-shard` | `blocked-by-evidence` | Run bounded depth-4 pilot shards as generated evidence before considering full depth-4 execution. | Keep remaining depth-4 shards gated unless a pilot or upstream lead produces prefix>=6, exact hits, or selected spans. |
| `seed-table-preset-probe` | `blocked-by-evidence` | Execute the top-ranked seed-table/Lotus preset evidence probe. | Do not promote a public Lotus preset unless ordinary held-out negative groups reach the gate and controls remain null. |
| `exact-short-hit-bundle-economics` | `blocked-by-evidence` | Execute the exact short-hit bundle economics evidence probe. | Do not promote span-3 hit bundling unless full-stream wins survive unrelated ordinary groups with null controls and non-comparable control density. |
| `whole-stream-residual-vector-probe` | `blocked-by-evidence` | Execute the whole-stream residual-vector falsification probe. | Do not continue residual-sidecar promotion unless whole-stream vector rows produce unrelated ordinary held-out wins with controls null. |
| `expander-salt-ensemble` | `blocked-by-evidence` | Execute the expander salt/preset ensemble falsification probe. | Do not promote salted expanders unless they beat equivalent random trials and create full-stream ordinary held-out wins with null controls. |
| `schema-native-public-dictionary-replication` | `blocked-by-evidence` | Replicate and harden the schema-native public dictionary result on frozen expansion corpora. | Do not promote a public dictionary registry or format-level support until paired shadow controls and external corpora separate cleanly. |
| `long-span-bundle-gate` | `blocked-by-evidence` | Gate broad long-span bundle sweeps before spending compute. | Do not run broad long-span bundle sweeps until the generated gate passes. |
| `recursive-structured-fixtures` | `blocked-by-evidence` | Gate recursive v2 claims against ordinary structured fixtures, not planted offsets. | Do not generalize recursive v2 gains unless ordinary non-offset fixtures produce verified later-layer container wins. |
| `fifth-byte-steering` | `blocked-by-evidence` | Test whether residual fifth-byte masks make prefix ladders survive held-out data. | Reject residual masks unless they generalize into prefix>=5 or exact hits. |
| `contextual-fifth-byte-steering` | `blocked-by-evidence` | Test whether context-conditioned fifth-byte masks beat same-shape null controls. | Reject contextual steering as a promotion path unless it beats null controls on held-out prefix>=5 or exact hits. |
| `structural-transform-search` | `blocked-by-evidence` | Test bounded structural reversible transforms across held-out, shadow, and binary controls. | Keep format-transform and broad depth work gated until structural or successor transforms produce repeatable promotion signals. |
| `byte-permutation-transform-search` | `blocked-by-evidence` | Test finite seed-output byte-distribution alignment with reversible global and phase-local byte permutations. | Treat byte-distribution alignment as null until it produces prefix>=5, exact hits, or metadata-profitable selected spans. |
| `bwt-mtf-transform-probe` | `blocked-by-evidence` | Test bounded BWT/MTF/RLE classic preconditioners without counting transform-only shortening as Lotus evidence. | Treat BWT/MTF/RLE as null seed-span evidence unless it produces prefix>=5, exact hits, or metadata-profitable selected spans. |
| `grammar-channel-match-discovery` | `blocked-by-evidence` | Test reversible grammar/channel streams with literal sidecars charged before promotion. | Treat grammar-channel sidecar models as null until a parser-backed channel survives held-out/control promotion. |
| `numeric-value-channel-match-discovery` | `blocked-by-evidence` | Test parsed numeric value streams with exact reconstruction metadata charged before promotion. | Treat parsed numeric value channels as null until exact seed spans survive full reconstruction metadata. |
| `record-context-transform-search` | `blocked-by-evidence` | Test record/context-aware reversible transforms with metadata charged before promotion. | Treat the current record-context lane as null until exact seed spans appear after metadata. |
| `token-dictionary-transform-search` | `blocked-by-evidence` | Test token/dictionary reversible transforms with dictionary metadata charged before promotion. | Treat the current token-dictionary lane as null until exact seed spans appear after metadata. |
| `affine-transform-search` | `blocked-by-evidence` | Test reversible affine byte remaps as a stronger alphabet-alignment preconditioner. | Treat prefix-4 movement as a steering clue only; require residual-sidecar savings, prefix>=5, or exact hits before depth escalation. |
| `seed-manifold-residual-steering` | `blocked-by-evidence` | Test whether forced exact seed spans plus residual sidecar bytes beat literal storage. | Do not promote residual steering until sidecar bytes plus metadata beat literal storage on held-out corpora. |
| `packed-sidecar-replication` | `blocked-by-evidence` | Replicate packed sidecar descriptor evidence on predeclared unrelated held-out corpora. | Find new independent match-discovery leads before more descriptor packing work. |
| `match-discovery` | `blocked-by-evidence` | Run pre-sidecar exact match discovery across validation and replication corpora. | Change match-discovery strategy before returning to sidecar packing or format work. |
| `alignment-arity-discovery` | `blocked-by-evidence` | Test whether the match-discovery null result is caused by block alignment, phase, or arity policy. | Find profitable exact hits or prefix>=5 movement before spending more on alignment-only tuning. |
| `transformed-match-discovery` | `blocked-by-evidence` | Run exact seed-span discovery after the frozen reversible transform-validation matrix. | Find a new transform family or deeper seed lane before promoting transform metadata. |
| `lead-exact-discovery` | `blocked-by-evidence` | Run exact seed-span discovery over selected affine, periodic, and composed transform leads. | Stop broadening selected prefix-4 leads until a new family or deeper seed lane crosses prefix>=5 or exact-hit thresholds. |

## Subagent Work Packages

| group | lanes | status counts | allowed now | guardrail |
| --- | --- | --- | --- | --- |
| `acceleration` | gpu-acceleration | blocked-by-evidence=1 | read-only audits and generated-artifact maintenance only | Do not launch broad compute, production GPU, or format promotion from this work package unless its generated promotion gate changes. |
| `compute-economics` | search-depth, scale-performance | gated=1, qualified=1 | read-only audits and generated-artifact maintenance only | Do not launch broad compute, production GPU, or format promotion from this work package unless its generated promotion gate changes. |
| `corpus-transform` | long-span-bundle-gate, heldout-corpora | blocked-by-evidence=1, gated=1 | read-only audits and generated-artifact maintenance only | Do not launch broad compute, production GPU, or format promotion from this work package unless its generated promotion gate changes. |
| `format-policy` | format-transforms | blocked-by-evidence=1 | read-only audits and generated-artifact maintenance only | Do not launch broad compute, production GPU, or format promotion from this work package unless its generated promotion gate changes. |
| `meta-research` | research-audit | qualified=1 | read-only audits and generated-artifact maintenance only | Do not launch broad compute, production GPU, or format promotion from this work package unless its generated promotion gate changes. |
| `operator-ui` | tauri-workflow | qualified=1 | read-only audits and generated-artifact maintenance only | Do not launch broad compute, production GPU, or format promotion from this work package unless its generated promotion gate changes. |

## Source Artifacts

The JSON `source_hashes` map pins this trigger board to the exact upstream evidence files below.
- `research_decision_sha256`: `4a51e19374ac30dd3ed06b584c924fe8261b4879080a7b25e1f428c08dbbcaa2`
- `experiment_queue_sha256`: `a7fa3696351787c1075e7cb1eef91279b73123fd7047f545140fea345b9cdf62`
- `goal_audit_sha256`: `f4ad8e687c50812d33252ef9a5e56bca7c64284c0e8846d846e99ac33a0a5607`
- `research_scorecard_sha256`: `96b2171667f1a9e7d23f13be73dde047e02e8fb9e6cd062b10427aba5e94eef0`
- `long_span_bundle_gate_sha256`: `68e74aede8d58394b117282713bb6001df946a3d4d8f717aaaa60f20ad3fcd76`
- `search_frontier_gate_sha256`: `9d39bd91dcd1d735b313fe9a40c82cec2ec37a97c5596b8ab30976d170bff9d5`
- `heldout_corpus_expansion_sha256`: `a6e91a2d9d2d96b89510e4a81a685983ef394be930bfc71fde2c970c5beed149`
- `acceleration_report_sha256`: `1e89e14b95fbe6824aa4fb8aa070327bcedbcc808e4fd6cb1b71931d1206f5f8`
- `recursive_structured_fixtures_sha256`: `2ab670c13d562e52f470ebec0ae5d61db29a82350c21780f064b741b3e1e0fac`
- `scale_performance_report_sha256`: `fe90df91a2f992edf6f10bc115517bd9870755fd4b5e85aa0ec9a09fde1b319d`
- `viability_sha256`: `3fb8c563c8f180263feba11c3b01bd6f7faafb17b7f1d57dee61f2428477a208`
